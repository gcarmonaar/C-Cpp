/*Este programa calcula la raíz cuadrada de 900.
La función sqrt toma un argumento del tipo double y devuelve uno del mismo tipo.
Los argumentos de las funciones pueden ser constantes, variables y expresiones*/
#include <stdio.h>
#include <math.h>

/*int main()
{
    printf("%.2f", sqrt(900.0));
    return 0;
}*/



int main()
{   float cl = 13.0, d = 3.0, f = 4.0;
    printf("%.f", sqrt(cl+d*f));
    return 0;
}